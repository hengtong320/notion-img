.PI*2, dist=22+Math.random()*50;
      const dx=Math.cos(a)*dist,dy=Math.sin(a)*dist;
      c.animate([{transform:"translate(0,0)",opacity:1},{transform:`translate(${dx}px,${dy}px)`,opacity:0}],{durat